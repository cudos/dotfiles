# dotfiles - my development environment configuration (MDEC)

Install ansible

```
$ sudo apt-get install ansible
```

Run the playbook:

```
$ cd ansible
$ ansible-playbook -K playbook.yml
```

Have fun!
